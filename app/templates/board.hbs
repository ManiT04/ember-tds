<div class="twelve wide stretched column">
  <div class="ui segment">
    <div  class="ui one column grid">
      <div class="column">
        <div class="ui attached segment"><i class="tags icon"></i>Commandes
          <LinkTo class="ui right floated teal button" @route="sections">Parcourir les sections</LinkTo>
        </div>
          <ul>
            {{#each @model.orders as |order|}}
              <li>
                <LinkTo @route="order" @model={{order.id}}>
                  {{order.dateCreation}} <span class="ui circular label">{{order.orderdetails.length}}</span>
                </LinkTo>
              </li>
            {{/each}}
          </ul>
      </div>

      <!--<LinkTo class="ui right floated teal button" @route="sections">Détails des commandes</LinkTo>

        {{#each @model.orders as |order|}}
          <div class="ui attached teal segment">
            <div class="column">{{order.orderdetails}}</div>
          </div>
        {{/each}}
        -->

    </div>
  </div>
</div>