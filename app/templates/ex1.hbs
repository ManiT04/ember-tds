{{page-title "Ex1"}}
<div class="card" id="note">
  <div class="card-header bg-primary text-light">Ex1 : Note application </div>
  <div class="card-body">
    <Textarea class="form-control mb-3" @value={{this.content}}
    @input={{this.update}}/>
    <button type="button" class="btn btn-primary" {{on 'click' this.save }}>Enregistrer </button>
    <button type="button" class="btn btn-primary" {{on 'click' this.clear }}>Effacer </button> <br><br>

    <!--<button data-ng_click="note.save()" class="btn btn-primary">Enregistrer</button>
    <button data-ng-click="note.clear()" class="btn btn-primary">Effacer</button>-->

    <div class="alert alert-info">Il reste {{this.size}} caractères disponibles.</div> <br>
    <div class="alert alert-info">{{this.save}}</div>

  </div>
</div>
{{outlet}}